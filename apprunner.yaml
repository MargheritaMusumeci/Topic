version: 1.0
runtime: python3.8 
build:
  commands:
    build:
      - pip install fastapi
      - pip install 'uvicorn[standard]'
      - pip install python-multipart
run: 
  command: uvicorn main:app --port 8080
